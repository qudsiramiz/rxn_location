You are an expert software developer and an expert in space physics. You are also an expert website designer and developer.
I have a streamlit based GUI here that I want to enahnce/add features too. [app.py, rx_model_funcs.py, jet_reversal_check_function.py]. Keep in mind that this will be published on PyPi for general use. Also please ensure it is easy to use and navigate for people who may not be familiar with the code. 

Can you please implement these requests:

1. Always keep a master list of all the times when reconnection jet was observed! I will let you decide what might be a good format for this (maybe json, or pickle or csv?)
2. Whenever a user checks if a jet was observed for a given time, add the time to the master list if it is not already there. Have a minute window around the given time to check for jets, i.e., if the user inputs a time, check the interval [time-1 minute, time+1 minute].
3. Do not add the time if it is already in the master list, even if the user checks for the same time again.
4. If the two jets were observed within 2 minutes of each other, you can be pretty sure that it is the same reconnection event and thus you should only add it to the master list once.
5. If the user inputs a time and there is already in the master list within 2 minutes of the user's input, show a dialogue box saying something along the lines of "a jet is present at {the exact time jet was observed"} and  ask the user if you would still like to generate jet reversal plot and/or reconnection model plot or both of them! Based on the user-input, run the appropriate codes!
6. Also add a mode where someone can pick a time range (or a start/end time and number of jets) and ask the GUI to make statistical plots for all the jets observed within that time range! This will mean that the master list of jets must have all the other data that are associated with statistical results csv.
7. Data Table: Display the master list in an interactive dataframe (using st.dataframe) where users can sort by time, probe, or other parameters.
8. Manual Pruning: Allow users to select rows and delete them if they determine a detected jet was a false positive upon closer inspection.
9. Export Options: Add buttons to export the current master list directly to CSV, JSON, or Pickle so users can easily take the data into their own external analysis scripts.
10. Save/Load Presets: Allow users to save their current sidebar configurations as a "Preset" (e.g., "Strict Jet Criteria", "T96 Fast Analysis") and load them from a dropdown. This saves time when switching between different types of events or data rates.
11. Dynamic Filtering: On the "Statistics Results" tab, add sliders to filter the statistical plots on the fly (e.g., only show stats for jets that lasted longer than 10 seconds, or occurred during high dynamic pressure).
12. Local Data Cache Management: Since pulling MMS and OMNI data can consume a lot of disk space and bandwidth:
Cache Dashboard: Add a small expander in the sidebar or a separate settings tab that shows the user how much disk space the local CDF files are taking up, with a button to "Clear Cache" or "Clear Data Older Than 30 Days". This is a massive quality-of-life feature for Python packages dealing with large datasets.
13. "Quick Re-run" from Master List: In the Master List Viewer (from suggestion #1), add a "Load into Dashboard" button next to each jet. Clicking this would automatically populate the sidebar with the exact time and parameters used for that specific jet and jump the user back to the controls to re-generate or tweak the models.