You are an expert software developer and an expert in space physics. You are also an expert website designer and developer.
I want you to build an interactive website tool where users can select/input a time range and a MMS probe number and then see the plots for that time range and MMS probe number. Keep in mind that this will be published on PyPi for general use. Also please ensure it is easy to use and navigate for people who may not be familiar with the code. 

1. The functionality should let the use check if for any time a jet was observed or not.
2. Whether a jet was observed or not, the  user should be able to plot and see both the figure as provided in @jet_reversal_check.py
3. If a jet was observed, then the user should have the capability to plot and see the images as generated by @rx_code.py
4. While generating the images as provided by @rx_code.py, the user should be able to select
    a. Tsyganenko model (t94, t01, t96 etc). Default is t96 
    b. Which MMS spacecraft (mms1-4) to use. Default is 1
    c. Which reconnection model to use (shear, bisection, maximum reconnection energy etc). User should be able to chose mutiple or all 4 models if desired. Default is shear
    d. Omni level (hro, lro). Default is hro
    e. m_p for Tsyganenko model (1-10). Default is 1
    f. dr for the grid size (0.1-1). Default is 0.25
    g. min_max_val for the range of the grid (-20 to 20). Default is 20
    h. y_min and y_max for the range of the grid (-20 to 20). Default is -20 to 20
    i. z_min and z_max for the range of the grid (-20 to 20). Default is -20 to 20
    j. save_data (True, False). Default is False
    k. nprocesses for the number of processes to use (1-16). Default is 1
    l. Draw patch (True, False). Default is True
    m. Draw ridge (True, False). Default is True
    n. Save fig (True, False). Default is True
    o. Fig name (string). Default is "crossing_all_ridge_plots"
    p. Fig format (png, pdf). Default is png
    q. Fig size (tuple). Default is (8.775, 10)
    r. Interpolation (string). Default is "None"
    s. Tsygy model (string). Default is "t96"
    t. Dark mode (bool). Default is False
    u. Save rc file (bool). Default is True
    v. Rc file name (string). Default is f"reconnection_line_data_mms{mms_probe_num}_{ind_range}.csv"
    w. Rc folder (string). Default is "data/rx_d/"
    x. Fig version (string). Default is f"v_{ind_range}"
5. All the images should be displayed in a way that the user can see them side by side. If the user selects multiple reconnection models, the plots should be displayed side by side for each model.

6. For the jet reversal check, the user should be able to select:
    a. Which MMS spacecraft (mms1-4) to use. Default is 1
    b. dt. Default is 300
    c. jet_len. Default is 3
    d. level. Default is "l2"
    e. coord_type. Default is "lmn"
    f. data_type. Default is ["dis-moms", "des-moms"]
    g. time_clip. Default is True
    h. latest_version. Default is False
7. All the images should be displayed in a way that the user can see them side by side.

8. The tool should also allow the user to download the plots as PNG or PDF files. The tool should be able to save all the plots generated in the current session and the user should be able to download the plots as a single zip file.